{% extends 'base.html.twig' %}

{% block title %}Improvment{% endblock %}

{% block body %}
    <h1>Improvment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ improvment.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ improvment.auteur }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ improvment.contenu }}</td>
            </tr>
            <tr>
                <th>Date_ajout</th>
                <td>{{ improvment.dateAjout ? improvment.dateAjout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('improvment_index') }}">back to list</a>

    <a href="{{ path('improvment_edit', {'id': improvment.id}) }}">edit</a>

    {{ include('improvment/_delete_form.html.twig') }}
{% endblock %}
