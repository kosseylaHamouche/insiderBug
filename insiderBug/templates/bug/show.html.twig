{% extends 'base.html.twig' %}

{% block title %}Bug{% endblock %}

{% block body %}
    <h1>Bug</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bug.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ bug.auteur }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ bug.contenu }}</td>
            </tr>
            <tr>
                <th>Date_ajout</th>
                <td>{{ bug.dateAjout ? bug.dateAjout|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bug_index') }}">back to list</a>

    <a href="{{ path('bug_edit', {'id': bug.id}) }}">edit</a>

    {{ include('bug/_delete_form.html.twig') }}
{% endblock %}
